# Tour Agent Assets (Campaign)

Per-country tour agent portraits for `public/scenarios/campaign/scenario.json`.

Folder layout:

- `dune/`
- `argos/`
- `verona/`
- `eleon/`
- `pandoria/` (placeholder for future integration)

You can replace each SVG with real photos later, keeping the same file names.
